// app/application/confirmation/page.tsx

export default function Confirmation() {
  return <div>Confirmation page</div>;
}
// TODO

// On success, the applicant is redirected to a confirmation page (e.g.
// /application/confirmation) which displays a confirmation number
// in the format of a GUID and a message that he has successfully applied
// for parental leave
